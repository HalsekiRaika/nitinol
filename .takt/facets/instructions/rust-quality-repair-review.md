元タスク、現在差分、関連する機械検査ログを読み、quality recovery変更を限定レビューしてください。

確認事項:

1. タスクが指定した一つの対象検査の根本原因へ対応しているか。
2. Workflowの限定quality gateが成功しているか。
3. 対象外failureまで修正してscopeを広げていないか。
4. warning抑制、検査除外、テスト弱体化で見かけ上PASSさせていないか。
5. 公開API、データ契約、module path、意味的挙動への副作用がないか。
6. full gateの残存failureを解消済みと誤記していないか。

対象検査の修復が妥当なら承認してください。修正可能な問題があればrepairへ戻し、
設計判断または対象拡張が必要ならABORT相当として明示してください。
